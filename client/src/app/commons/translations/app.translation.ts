export interface AppTranslation {
    mainToolbar: {
        menus: {
            goTo: string;
            home: string;
        },
        buttons: {
            goHome: {
                label: string;
            }
        }
    }
}